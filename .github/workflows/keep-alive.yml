name: Keep Alive

on:
  schedule:
    - cron: '0 */12 * * *'  # 12시간마다 실행 (UTC 기준)
  workflow_dispatch:  # 수동 실행도 가능

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Wake up app
        run: |
          curl -X GET https://jeniapp.streamlit.app/